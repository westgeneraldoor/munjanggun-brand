# 문장군 디자인 안티패턴

> 기준 문서: `DESIGN.md` v5.0 Editorial Showroom

## 할인 전단지형 화면

빨간 가격, 번쩍이는 배지, 과한 취소선, 근거 없는 최저가 문구를 반복하지 않습니다. 가격은 숨기지 않되 현장 조건과 확인 범위를 함께 설명합니다.

## 가짜 로고와 장식 마크

정식 그래픽 로고가 없는 상태에서 문 모양 SVG, 방패, 왕관, 이니셜 마크를 임의로 만들지 않습니다. `문장군 / MUNJANGGUN` 타입 락업을 사용합니다.

## 생활앱형 과잉 라운드

모든 카드와 섹션을 둥근 캡슐로 만들지 않습니다. CTA는 pill, 필드는 부드러운 radius, 편집 카드와 이미지 프레임은 더 단정한 radius를 사용해 위계를 만듭니다.

## 색만으로 말하는 상태

오류·성공·선택 상태를 빨강·초록만으로 구분하지 않습니다. 텍스트, 아이콘, `aria-invalid`, `aria-pressed`, live region을 함께 사용합니다.

## 제품부터 확정시키는 상담

상담 시작부터 제품명을 강제하거나 방문실측을 자동 견적처럼 설명하지 않습니다. `관심 있는 항목`과 `잘 모르겠어요`를 제공하고 집 구조와 고객 불안을 함께 확인합니다.

## 차갑고 사람 없는 카탈로그

제품만 흰 배경에 잘라 나열하지 않습니다. 밝은 실제 주거 공간과 생활 맥락이 보이는 이미지를 우선하고, 이미지 위 텍스트는 제품의 핵심 부분을 가리지 않습니다.

## 하드코딩 복제

HEX, rgba, 그림자, 간격, radius, 폰트 크기를 화면 파일마다 복제하지 않습니다. `tokens/brand.css`의 semantic·component 토큰을 사용합니다. breakpoint, gradient stop, object-position 같은 예외는 `DESIGN.md`의 하드코딩 정책을 따릅니다.

## 동작 없는 시각 모형

클릭 가능한 것처럼 보이는 카드, 빈 `href`, 없는 앵커, 열리지 않는 범용 내비게이션, 색만 바뀌는 가짜 테마를 두지 않습니다. 실제 루트 요소, 계산 색상, 키보드 조작을 검증합니다. 실제 제품 화면과 고객 여정은 하위 프로젝트에서 설계하며 중앙 플레이그라운드에 재현하지 않습니다.
