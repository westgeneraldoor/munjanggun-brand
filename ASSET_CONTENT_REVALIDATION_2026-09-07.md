# 2026-09-07 자산 내용 재검증 기록

> 대상: `INTAKE-20260904-01`의 고유 시각 자산 407개
> 결론: `verified-v1`~`verified-v4` 모두 감사 이력으로만 보존한다. 독립 감찰에서 v4의 명백한 내용 오류와 증거 범위 과장이 확인되어 현재 적용 가능한 content authority는 없다.

## 2026-09-08 원문 원장 파일럿

기존 구간 배정 기록을 자산별 판독 원문으로 소급 해석하지 않고, 두 intake를 합친 409개 정지 이미지 중 첫 12개를 새로 독립 판독했다. 1차 12건·2차 12건의 실제 관찰 원문을 비공개 Z에 보존하고, 원본 대조 교정본 12개를 별도로 추가했다. 작성자 3명과 별도 교정자의 Ed25519 키, 원문 바이트 SHA, 원본 SHA, 대기열과 pair 연결은 `assets:validate-raw-review-ledger -- --mode pilot-complete`로 검증한다.

이 파일럿은 `complete_non_authority`다. `선 하나로`, `MAPLE` 확정 철회, `눈을 넓히고`, 우측 상단 `문장군` 같은 확인 항목을 원본에 맞게 처리했지만, 남은 397개와 GIF 2차 검토·최종 봉인은 완료되지 않았다. 공용 검색·추천·handoff·외부 추출 차단은 유지한다.

## verified-v4 철회

2026-09-07 독립 감찰에서 다음 결함이 재현되어 v4를 즉시 철회했다.

- 글자가 없는 `3연동중문/컬렉션/베이직/썸네일/004.jpg`에 `BASIC; FULL WINDOW`를 보이는 문구로 기록하고 A/S 태그를 부여함
- `AS` 단순 부분 문자열이 `BASIC`, `GLASS`, `ASH`에 일치해 색상표와 디자인표가 A/S 검색 결과에 포함됨
- `원` 단순 부분 문자열이 상품명 `원슬라이딩`에 일치해 가격 없는 자료가 가격 검색 결과에 포함됨
- GIF 72개의 6,162개 원본 프레임 중 605개를 선택 판독했으나 전체 프레임 또는 전체 재생을 육안 확인한 것처럼 기록함
- 정지 이미지 149개의 검토 시각이 v4 봉인 시각 이후로 기록돼 검토 후 봉인 순서를 증명하지 못함

원본 407개와 1,134개 논리 경로의 SHA·크기, 사용권 결정, 공개 Git 보류 상태는 이번 철회로 바뀌지 않는다. 새 봉인은 픽셀에서 직접 읽은 `visibleText`, 경로·폴더에서 얻은 `sourceContext`, 추론 설명을 분리하고 각 태그의 근거를 검증하며 `reviewedAt <= sealedAt`를 만족한 뒤에만 검토한다.

## 발생 원인

기존 `semanticSummary`, `ocrText`, `claimSignals`는 영구 생성기가 아닌 일회성 OCR·키워드 분류 결과에서 시작됐다. 원화 기호 `₩`가 가격 판정식에서 빠졌고, `소프트아치` 같은 디자인명을 동작 표현으로 오인했다. 병합기와 완료 검사기는 내용의 원본 일치가 아니라 필드 존재와 상태값 일치만 확인해 오류를 막지 못했다.

| 원본 | 기존 오분류 | 재판독 결과 |
| --- | --- | --- |
| `3연동중문/014.png` | 유리 선택 이미지 | 3연동 디자인 10종과 60만~90만원 가격표 |
| `3연동중문/017.png` | 개폐 기능 이미지 | 아치 디자인 4종과 각 가격표 |
| `스윙중문/013.jpg` | 개폐 기능 이미지 | 스윙도어 아치 디자인 4종 안내 |
| `3연동 자동중문/020.jpg` | 일반 3연동 이미지 | 자동중문 디자인 8종과 104만·109만원 가격표 |
| `3연동 자동중문/026.png` | 일반 3연동 이미지 | 자동중문 아치 4종과 104만~119만원 가격표 |

## 현재 재검증 진행 범위

| 구분 | 수량 | 확인 방식 |
| --- | ---: | --- |
| 정지 이미지 | 335 | 185개 기존 문자 기록군과 150개 기존 무문자 기록군의 원본 의미 재판독 완료. 확대 대상 89개 포함 원본 SHA 불일치 0. 결과는 private 후보이며 새 crop·서명 계약으로 아직 봉인하지 않음 |
| GIF | 72 | 72개·6,162프레임의 디코딩 수치는 재확인하고 72개 모두 시간축 표본을 의미 판독. 연속 전체 재생 또는 모든 프레임 확인 증거가 없어 전부 `needs_escalation` |
| 합계 | 407 | 원본 경로·SHA·sourceRefs는 대조하되, 내용 판독과 운영 승격은 모든 불확실 항목이 해소될 때까지 분리 |

범위 조사에서는 경로 문자열 기반 오분류 영향군을 확인했다. 잘못된 A/S 태그 영향은 11개 SHA·20경로, 잘못된 가격 태그 영향은 4개 SHA·8경로이며 합집합은 14개 SHA·26경로다. 또한 v4에서 `visibleText` 또는 OCR이 비어 있지 않은 240개 SHA·841경로 중 정지 이미지는 185개였고, v4에서 무문자로 취급한 정지 이미지 150개 중 103개에서 실제 문자를 확인했다. 확대 대상 89개는 1~45와 46~89 두 묶음으로 원본·sourceRefs·crop SHA를 다시 확인했으며 불일치 0이다. 원본에서도 의도적으로 흐리거나 가려져 정확 전사할 수 없는 6개 영역은 빈칸을 추측으로 채우지 않고 별도 불확실 기록으로 남겼다.

185개 문자 기록군의 새 의미 판독은 62·62·61개 세 묶음으로 185개 SHA를 정확히 한 번씩 포함하며 Z 원본 해시 불일치 0, 누락 0, 운영 상태 항목 0으로 재검증했다. 150개 기존 무문자 기록군을 더한 정지 이미지 후보 범위는 335개다. 이 수치는 원본을 다시 본 private 판독 진척이지 새 운영 authority가 아니다. 정지 문구의 결정론적 crop과 민감 문구의 서명된 독립 2차 검토, GIF 연속 재생 또는 전체 프레임 증거가 준비되지 않은 항목은 `needs_escalation`으로 남기며 현재 공용 검색·추천·handoff 차단을 해제하지 않는다.

## 새 통제

- 기존 catalog의 식별자, object, 원래 경로, 권리 기록은 유지한다.
- 잘못된 내용 필드는 private content overlay로만 대체한다.
- overlay는 407개 SHA를 정확히 한 번씩 포함해야 한다.
- 보이는 문구는 `visibleText`, 원본 경로·폴더 맥락은 `sourceContext`, OCR 초안은 `ocrText`, 추론은 `inferredText`로 분리한다.
- `visibleText`마다 원본 SHA, 정지 픽셀 또는 GIF 프레임, 정규화 좌표, 증거 파일 경로·SHA를 연결한다. 무문자는 `textPresence: none_observed`로 명시한다.
- 정지 이미지의 문구 영역은 원본의 정규화 좌표에서 PNG/JPEG를 고정 버전으로 다시 디코딩해 crop RGBA·크기·픽셀 SHA를 재계산한다. 제출 crop이 해당 원본 영역과 다르면 봉인하지 않는다.
- 모든 1차 판독 원문은 등록된 판독자 principal의 Ed25519 서명을 요구한다. 봉인본에는 원문 자체와 원문 SHA를 함께 보존하고, 실제 사용 시 서명·원문 SHA·shard 대상 SHA 집합을 다시 검증한다.
- `verified` 판정에는 불확실 항목이 하나도 남아 있지 않아야 한다. `uncertainties`가 비어 있지 않으면 상태값과 무관하게 봉인을 거부한다.
- 모든 정지 이미지는 원본 SHA와 디코딩된 원본 크기·전체 픽셀 SHA를 고정하고, 겹치지 않는 native-resolution 타일이 원본의 모든 픽셀을 정확히 한 번 덮는 coverage manifest를 요구한다. 생성기와 소비자는 manifest·타일 파일 SHA, 각 타일 크기·픽셀 SHA, 원본 영역과 canonical PNG 바이트, 전체 면적과 무겹침을 다시 검증한다.
- 모든 정지 이미지와 GIF는 1차 판독자와 다른 principal이면서 다른 공개키 fingerprint를 사용하는 독립 2차 의미 판정 영수증을 요구한다. 이 서명은 원본 SHA, 1차 결정 digest, 설명, 콘텐츠 유형, 용도, 검색 태그, 문자 존재 여부·문구, 개인정보 신호, 불확실 항목 전체에 결속된다.
- 가격·A/S·행사·스펙·리뷰 수·시공 일정 등 민감 claim의 문구는 1차 판독자와 다른 등록 판독자의 Ed25519 서명 2차 확인 영수증이 같은 원본 SHA·문구·영역·crop/frame 증거에 결속돼야 한다. 이름뿐 아니라 정규화 principal과 canonical SPKI DER로 계산한 공개키 fingerprint가 중복되지 않아야 한다. PEM 줄바꿈이나 공백 표기만 바꾼 동일 키도 같은 키로 판정한다. 서명 신뢰 목록도 authority snapshot과 품질 정책 SHA에 함께 묶는다.
- crop 검증은 “이 원본의 이 영역을 보았다”는 증거다. 문구를 사람이 정확히 읽었다는 사실을 OCR이 자동 증명하는 것으로 과장하지 않으며, 민감 문구는 독립 2차 판독으로 보완한다.
- GIF는 디코딩 메타데이터, 표본 프레임, 연속 전체 재생, 모든 디코딩 프레임 판독을 서로 다른 사실로 기록한다.
- GIF 표본은 프레임 번호·증거 파일 경로·증거 SHA·원본 SHA를 연결하고, 연속 전체 재생은 별도 관찰 영수증을 해시로 봉인한다. 공용 생성기와 소비자도 workbench 영수증의 event digest·event count·시간 순서·가시성·포커스·중단 이벤트 0건을 재검증한다.
- 표본 프레임만 확인한 GIF는 전체 재생 또는 전체 프레임 검토 완료로 승격할 수 없다.
- 보이는 가격 문구에 가격 claim 신호가 없으면 생성이 실패한다.
- 원본 파일, object 파일, sourceRefs, review shard, 결정 해시, overlay, receipt의 연결을 검증한다.
- 단일 상품 source는 해당 상품의 필수 이름을 포함해야 하며, 교차상품은 원본에서 실제 확인되고 기존 sourceRefs와 중복되지 않을 때만 선언한다.
- 공용 ABS 이미지는 세부 서비스명으로 강제 분류하지 않는다. profile이 허용한 `ABS도어` family와 원본 판독 사유가 함께 봉인된 경우에만 generic 판정을 허용한다.
- intake profile 원본 바이트를 private authority 안에 snapshot으로 보존하고 receipt가 경로·SHA를 함께 고정한다.
- 품질 정책에 등록되지 않은 catalog는 기본 거부한다.
- 공용 검색, 직접 검색, 블로그 선택, handoff, 외부 추출은 같은 품질 게이트를 통과해야 한다.
- handoff는 호출자가 넘긴 파일 경로·출처·발행 상태를 신뢰하지 않고 검증된 overlay와 object store에서 선택 결과를 다시 구성한다.
- `BASIC`, `GLASS`, `ASH`의 일부 문자열을 A/S로, `원슬라이딩`의 `원`을 가격으로 간주하는 검색·태깅은 회귀 테스트로 거부한다. 세부 검색 조건은 전체 후보에 먼저 적용한 뒤 결과 수를 제한한다.
- 기존 Z 보고서와 catalog 내용 필드는 `superseded_invalid_semantics` 감사 이력으로 보존하며 덮어쓰지 않는다.

## 다음 봉인 기준

공개 Git에는 이미지·GIF나 private overlay를 넣지 않는다. Git에는 검증 코드, 스키마, 정책의 SHA와 운영 기록만 둔다.

이전 v4 overlay·receipt 해시는 철회된 감사 이력일 뿐 현재 봉인값으로 재사용하지 않는다. 새 `content-evidence-v4` authority는 407개 전부의 새 증거, 정지 이미지 전체 타일 커버리지, 전 자산의 독립 2차 의미 판정, GIF 전체 재생 증거가 준비되고 불확실 항목이 0개가 된 뒤 현재 코드로 처음부터 다시 생성한다. 새 해시가 생기기 전에는 `visually_verified` 상태를 기록하지 않는다.

내용 정확성과 별도로 가격·행사·스펙의 최신성, 개인정보, 외부 발행 조건은 계속 독립 게이트를 통과해야 한다.

사용권 결정은 다시 받지 않는다. 문장군 자체 제작, 비공개 Codex 공용 소스 사용, 블로그·SNS 재사용 승인, 공개 Git 보류라는 기존 사장 지시는 그대로 유지한다. 가격·행사·스펙의 최신성 판단과 공개 Git 저장 가능 여부는 내용 정확성과 별도 상태다.
